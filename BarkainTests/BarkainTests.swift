// This file intentionally left minimal.
// Tests are organized under Features/ and Services/ directories.

import XCTest
@testable import Barkain

final class BarkainTests: XCTestCase {
    func test_appLaunches() {
        // Placeholder — verifies the test target links correctly
        XCTAssertTrue(true)
    }
}
