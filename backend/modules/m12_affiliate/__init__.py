"""M12 Affiliate — URL tagging + click tracking.

Deterministic, zero-LLM. `AffiliateService.build_affiliate_url` is a pure
static method so URL tagging can be validated without a database fixture.

See `docs/ARCHITECTURE.md` and `docs/CHANGELOG.md` §Step 2g for context.
"""
